{
  "name": "reading-hub",
  "version": "1.0.0",
  "description": "Multi-Grade Reading HUB (Grades 1, 2, and 3)",
  "main": "server/index.js",
  "scripts": {
    "build": "npm --prefix client install && npm --prefix client run build",
    "start": "node server/index.js"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3"
  }
}
